import {StyleSheet} from 'react-native'
import {colors} from '../../../Styles'

const styles = StyleSheet.create({
  commentRegistWrapper: {
    position: 'relative',
  },

  registWrapper: {
    flexDirection: 'row',
    padding: 10,
    alignItems: 'center',
  },

  keyboardWrapper: {
    alignItems: 'flex-end',
  },
})

export default styles
