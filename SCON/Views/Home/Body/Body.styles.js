import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  bodyWrapper: {
    position: 'relative',
    flex: 1,
    paddingVertical: 40,
  },
  swiperWrapper: {},
});

export default styles;
