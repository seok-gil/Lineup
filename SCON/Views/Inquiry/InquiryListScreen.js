import React, { Component } from 'react';
import { createStackNavigator } from '@react-navigation/stack';

import {
	Button,
	View,
	Text,
	StyleSheet,
} from 'react-native';

export function InquiryListScreen({ navigation }) {
	return (
		<View >
			<Text>
				문의리스트
			</Text>
		</View>
	);
}
